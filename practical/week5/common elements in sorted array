#include<iostream>
using namespace std;
int main(){
    int n,m,i,j=0,k=0;
    cin>>n;
    cin>>m;
    int a[n];
    int b[n];
    cout<< "Enter the numbers in the array1\n";
    for(i=0;i<n;i++)
        cin>>a[i];
    cout<< "Enter the numbers in the array2\n";
    for(i=0;i<m;i++)
        cin>>b[i];
    i=0;
    cout<< "The common elements are\n";
    while(i<n){
        if(a[k]==b[j]){
            cout<<b[j] << " ";
            j++;k++;
        }
        else if(a[k]<b[j])
            k++;
        else
            j++;
        i++;
    }
    return 0;
}
